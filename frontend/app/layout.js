export const metadata = {
  title: "BillSenser",
  description: "AI Powered Utility Bill Intelligence"
}

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  )
}